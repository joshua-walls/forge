# Starter Schema

Copy this note body into:

```text
{{schemaFile}}
```

```md
---
type: reference
status: active
tags:
  - tool/forge
  - topic/schema
created: {{today}}
updated: {{today}}
ai_private: false
review_cycle: never
---

# Vault Schema

version:: "1.0"

# Contract

```yaml
meta:
  author: vault
  schemaRef: "{{schemaFile}}"

required_fields:
  - name: type
    type: enum
    values: [reference, procedure, project, concept]
    severity: error

  - name: status
    type: enum
    values: [draft, active, complete, archived]
    severity: error

  - name: tags
    type: list
    min_items: 1
    severity: error

  - name: created
    type: date
    severity: warning

  - name: updated
    type: date
    stale_after_days: 90
    severity: warning

  - name: ai_private
    type: boolean
    severity: warning

  - name: review_cycle
    type: enum
    values: [daily, weekly, monthly, quarterly, yearly, never]
    severity: warning

optional_fields:
  - name: domain
    type: string
    severity: info

  - name: project
    type: string
    severity: info
    lint_rules:
      - rule: required_when
        field: type
        equals: [project]
        severity: warning

inline_fields:
  - source
  - version
  - schema_version
  - summary

tag_rules:
  require_namespace: true
  unknown_tags: warning
  severity: warning
  allowed_namespaces:
    - meta
    - source
    - tool
    - topic
    - project

exempt_paths:
  - "{{forge}}"
  - "System/Templates"
```
```
