# Move Notes Patch

This moves notes from an inbox staging area into a destination folder while preserving paths below the source root.

```yaml
meta:
  description: Move processed inbox notes

operations:
  - op: move_note
    target_pattern: "{{inboxFolder}}/**/*.md"
    source_root: "{{inboxFolder}}"
    destination_folder: "Notes"
    frontmatter:
      status: active
      updated: {{today}}
```

Do not use `frontmatter` together with `strip_frontmatter`.

---

## Related Notes

- [[{{docsFolder}}/reference/3. Patch Engine|Patch Engine]]
- [[{{docsFolder}}/reference/5. Maintenance|Maintenance]]
- [[{{examplesFolder}}/workflows/2. Monthly Maintenance|Monthly Maintenance]]
